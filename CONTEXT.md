# TACZ: Z-Tweaks

TACZ 的**客户端**附属模组：接管改装界面，提供更好的武器预览、更简单的改装流程、更易阅读的配件简介与参数。

## Language

**改装 (Refit)**:
把配件安装到枪械上的行为。
_Avoid_: 改造、改枪、升级

**改装界面 (Refit Screen)**:
承载改装的 GUI。**原生界面**指 TACZ 自带的改装界面；**Z-Tweaks 界面**指本项目替换后的界面。
_Avoid_: 改装台（那指合成台）、工作台

**接管 (Takeover)**:
用 Z-Tweaks 界面替换原生界面，成为改装的默认入口。
_Avoid_: 覆盖、替换

**配件 (Attachment)**:
可安装到枪械上的物品。
_Avoid_: 附件、部件、模块

**槽位 (Slot)**:
枪械上按配件类型划分的安装位；TACZ 每个类型只有一个槽位。
_Avoid_: 插槽、挂点

**概览态 (Overview)**:
改装界面中未选中任何槽位的状态；原生实现在此状态承载镭射颜色调色。
_Avoid_: NONE 态（代码里叫 `NONE`，语言里一律用"概览态"）

**可安装 (AllowAttachment)**:
判定某配件能否装到某枪上：类型白名单 + 标签匹配。
_Avoid_: 兼容（"兼容"用于模组/环境层面）

**不可安装 (Uninstallable)**:
判定某配件装不到某枪上的状态；候选列表里置底 + 分隔线 + 灰显，角标两档——「装不上这个配件」（枪有白名单但不含它）/「这里装不了配件」（枪压根没声明白名单）。只在玩家**拥有**该配件时列出；**可看不可装**（能选中看描述，但不驱动 3D 预览与参数，安装一律拒绝）。
_Avoid_: 不兼容（那指模组 / 环境层面）、不支持（"不支持"属于槽位那一层："这把枪不支持 X 槽位"）

**候选配件 (Candidate Attachment)**:
玩家在配件列表中选中、尚未安装的配件。
_Avoid_: 待装配件

**裸枪 (Bare Gun)**:
未安装任何配件的枪械；本项目参数比较的基准。
_Avoid_: 原厂枪、白板枪

**属性修改器 (Modifier)**:
配件数据中对某一属性的增减描述（加值 / 百分比 / 倍率）。
_Avoid_: 属性、增益

**参数 (Stat)**:
展示给玩家的数值（伤害、射速、后坐力等）。数据层的对应物叫**属性修改器**。
_Avoid_: 属性（易与 modifier 混淆）

**Pros/Cons**:
由属性修改器自动推导出的优点 / 缺点列表，展示在候选配件的详情中。
_Avoid_: 优缺点列表、增益减益

**配件锁 (Attachment Lock)**:
枪械上的一个标志位；锁定的枪不允许打开改装界面。
_Avoid_: 锁定、上锁（易与过热锁混淆）

**收藏 (Favorite)**:
玩家对某个**配件**的标记（不是"某把枪上的某个配件"），用来在候选列表里优先展示。键是配件 id，跨枪、跨槽位通用；它排在"已拥有"之后——可用性优先于偏好。
_Avoid_: 偏好、喜爱、星标（星标只是它的图标）

**配件元数据 (Attachment Metadata)**:
描述某个配件的补充内容——分类文件夹、按钮短名、手写优缺点、单位覆盖、背景资料。**本项目不引入**：分类只用 AttachmentType，文案交枪包作者（见 ADR-0001）。开发计划 §3.1 曾以此为核心，该节已整节作废。
_Avoid_: 扩展元数据、meta（口语里 meta 还指别的，别用）、标签（那是 TACZ 的 tag，是兼容关系不是分类）

**预设 (Preset)**:
一套「槽位 → 配件 id」的装配记录，按枪 id 存在玩家状态根里（`ztweaks/presets/<namespace>/<path>/<名>.json`）。**只记 id**：瞄具档位、镭射色这类配件自带的状态随实物走，写进预设也送不进去。**应用即全集**——应用后枪的装配完全等于预设，预设没提到的槽位会被卸下；应用前先干跑一次，把「装上 / 卸下 / 缺件」列清让玩家确认，缺件要说出来但不中止应用。
_Avoid_: 配置（那是 Forge ModConfig）、方案、build
